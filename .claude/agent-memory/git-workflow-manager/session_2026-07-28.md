---
name: 2026-07-28 뮤직 플레이어 & /photos UI 개편 세션
description: MusicPlayer 신규, /photos Swiper 캐러셀, 전역 레이아웃 리팩터링, code-reviewer 3건 버그 수정
metadata:
  type: project
---

## 세션 요약
**날짜:** 2026-07-28  
**작업:** MusicPlayer 신규 구현, /photos 페이지 UI 전면 개편, 전역 레이아웃 구조화  
**상태:** code-reviewer 리뷰 완료, 지적 3건 수정 완료

## 신규 파일 (7개)
1. `components/MusicPlayer.tsx` — 재사용 뮤직 플레이어 (공통 props)
2. `components/MusicPlayerDock.tsx` — 전역 고정 배치 (bottom offset 동적)
3. `components/PhotoCarousel.tsx` — Swiper EffectCoverflow 캐러셀
4. `components/DesktopHero.tsx` — 왼쪽 패널 (데스크탑, /photos 제외)
5. `components/AppBackground.tsx` — 전체화면 배경
6. `components/AppPanel.tsx` — 오른쪽 앱 패널 (/photos는 w-full)
7. `public/audio/songs/song.mp3` — 뮤직 플레이어 오디오

## 수정 파일 (4개)
1. `components/PhotoPasswordGate.tsx` — NEXT_PUBLIC_PHOTOS_PASSWORD 환경변수화
2. `components/BottomNav.tsx` — /photos에서 숨기기
3. `app/layout.tsx` — MusicPlayerDock 추가, Service Worker 프로덕션 조건 수정
4. `package.json` — swiper 패키지 추가

## Code-Reviewer 지적 3건 (모두 수정 완료)
1. ✓ AppPanel의 /photos 분기에 `lg:relative` 누락 → 추가 (ConfirmDialog containing-block 계약)
2. ✓ MusicPlayer 하단 56px 불필요 패딩 → 제거
3. ✓ PhotoCarousel 이미지 alt 빈 값 → `{photo.date}` 추가

## 커밋 전략
- 3개 커밋으로 분리: 뮤직플레이어, UI/레이아웃, swiper 패키지
- 각 커밋은 기능 단위로 명확하게 분리

## 비밀값 검증
- ✓ .env에만 `NEXT_PUBLIC_PHOTOS_PASSWORD=...` 
- ✓ 코드에는 `process.env.NEXT_PUBLIC_PHOTOS_PASSWORD` 참조만 (평문 없음)
- ✓ 안전 확인됨
